# Core pipeline libraries
google-generativeai
jsonschema
yt-dlp
moviepy
numpy # Essential for moviepy and other numerical tasks
groq
websocket-client
Pillow
openai-whisper
requests
pyspellchecker
python-dotenv
termcolor

# TTS specific libraries
kokoro-onnx
onnxruntime

# Notes on other potential dependencies (user may need to install separately if features are used):
# - Libraries like torch, torchaudio, torchvision are large and might be installed
#   as part of specific model requirements (e.g., for local Whisper or Kokoro if they need it).
#   It's good to let the user install them if/when a component explicitly requires them.
#
# To install these requirements:
# pip install -r requirements.txt
